apiVersion: v1
kind: Service
metadata:
  name: fat-service
spec:
  type: NodePort  
  selector:
    app: fat
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 8080  
      nodePort: 30006   